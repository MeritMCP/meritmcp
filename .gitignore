node_modules/
dist/
*.log
.DS_Store
# Merit run artifacts (the snapshot IS committed; results are not)
results/
merit.json
*.sarif

# Internal strategy / working notes — kept private (not for the public repo)
CLAUDE.md
RESEARCH.md
05-ROADMAP-AND-DECISIONS.md
06-EDGE-AND-MOAT.md

# Exposé: the scanner scripts ARE committed (run in CI sandbox); generated/local artifacts are not
scripts/servers.json
scripts/scan-workspace/
LEADERBOARD.md
expose-results.json

# Guard: probe-canary artifacts a misbehaving server might create in the cwd
*MCPV*
